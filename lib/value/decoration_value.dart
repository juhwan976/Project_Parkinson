import 'package:flutter/material.dart';
import 'package:project_parkinson/value/screen_value.dart';

final buttonSize = appWidth * 0.4;

const boxPadding = EdgeInsets.all(20);

final boxBorderRadius = BorderRadius.circular(appWidth * 0.05);
final boxBorderRadiusValue = appWidth * 0.05;
